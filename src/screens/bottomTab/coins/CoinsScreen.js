import React, {useState, useRef} from 'react';
import {View, Pressable, Text, Image} from 'react-native';

import styles from './CoinsStyle';

function CoinsScreen() {
  return <View style={styles.container}>
    
  </View>;
}

export default CoinsScreen;
